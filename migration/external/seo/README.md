# External SEO Exports

Drop real launch exports in this directory with these exact names:

- `search-console.csv`
- `yandex-webmaster.csv`
- `backlinks.csv`
- `analytics.csv` optional, privacy event ledger is already accepted

The real `.csv` files are gitignored. Keep the `.csv.example` files committed
as import templates only.
